# Segmentation metrics on the fixed test set (per class)

PENDING — needs: outputs/05_predictions/test_metrics.json without a per_class block — re-run gsv4.train.evaluate_test on the workstation (validation pass)
