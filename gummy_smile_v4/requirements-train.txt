# gummy_smile_v4 — training environment (Linux workstation, NVIDIA RTX 5090 / Blackwell, Python 3.11)
# Install with the CUDA 12.8 wheel index (Blackwell needs torch >= 2.7 built for cu128):
#   pip install -r requirements-train.txt --extra-index-url https://download.pytorch.org/whl/cu128
-r requirements.txt
torch>=2.7.0
torchvision>=0.22.0
ultralytics>=8.3.100
tabulate==0.9.0
